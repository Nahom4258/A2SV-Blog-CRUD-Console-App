using A2SV___Blog_CRUD;
using Microsoft.EntityFrameworkCore;
using Xunit;
using A2SV___Blog_CRUD.Models;

namespace Tests;

public class UnitTest1
{
    // [Fact]
    // public void Test1()
    // {
    //     var optionsBuilder = new DbContextOptionsBuilder<A2svBackendLearningContext>()
    //         .UseInMemoryDatabase(Guid.NewGuid().ToString()); // Use a unique name for the in-memory database
    //
    //     using var _context = new A2svBackendLearningContext(optionsBuilder.Options);
    //
    //     var repo = new PostManager(_context);
    //     repo.CreatePost("title", "content");
    //
    //     Assert.Single(_context.Posts);
    //     Assert.Equal("title", _context.Posts.First().Title);
    //     Assert.Equal("content", _context.Posts.First().Content);
    //     Assert.Equal(1, repo.ReadAllPosts().Count);
    // }
    
    
}